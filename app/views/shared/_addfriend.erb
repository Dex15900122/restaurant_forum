<% if user != current_user %>
  <% if current_user.friends?(user) %>
    <%= link_to '解除好友', friendship_path(user), method: :delete , class:"btn btn-link"%>
  <%else%>
    <%= link_to '加好友', friendships_path(friend_id: user), method: :post, class:"btn btn-default" %>
  <%end%>
<%end%>